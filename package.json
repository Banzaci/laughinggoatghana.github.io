{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"laughinggoatghana.com\" >> out/CNAME git add out && git commit -m \"Deploy Next.js to laughinggoatghana\" && git subtree push --prefix out origin gh-pages",
    "deploy": "npm run build && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "fetch": "^1.1.0",
    "instafeed.js": "^1.4.1",
    "node-fetch": "^2.3.0",
    "react": "^16.6.3"
  },
  "devDependencies": {
    "styled-jsx": "^3.1.2",
    "webpack": "^4.27.0"
  }
}
