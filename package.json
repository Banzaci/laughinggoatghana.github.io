{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "deploy": "next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"laughinggoatghana.com\" >> out/CNAME && push-dir --dir=out --branch=laughinggoatghana.github.io --cleanup --verbose --remote=github-io",
  },
  "dependencies": {
    "fetch": "^1.1.0",
    "instafeed.js": "^1.4.1",
    "node-fetch": "^2.3.0",
    "react": "^16.6.3"
  }
}
