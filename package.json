{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "deploy": "npm run build && npm run export && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "fetch": "^1.1.0",
    "instafeed.js": "^1.4.1",
    "node-fetch": "^2.3.0",
    "react": "^16.6.3",
    "react-ga": "^2.5.6"
  },
  "devDependencies": {
    "styled-jsx": "^3.1.2",
    "webpack": "^4.27.0"
  }
}
